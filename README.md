## React Native Prevent Recent Screenshots

Usage
- Android, simply import `import "react-native-prevent-recent-screenshots"`
- iOS, import `import PreventRecentScreenshots from "react-native-prevent-recent-screenshots";`, call `shouldPreventRecentScreenshot(true | false)` whenever this functionality needs to be disabled
